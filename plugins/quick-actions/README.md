# @vibe-ctl/plugin-quick-actions

Common action widgets for vibe-ctl. Thin buttons and shortcut tiles
that call into other plugins' services: start a new Claude Code
session, spawn a terminal, open a project. Designed for canvas placement
so users can pin frequently-used actions next to their work.
